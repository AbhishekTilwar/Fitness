name: phone_life_ai
description: Phone behavior to life optimization — passive signals, AI daily plan.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  permission_handler: ^11.3.1
  pedometer: ^4.0.2
  sensors_plus: ^5.0.1
  battery_plus: ^6.0.3
  shared_preferences: ^2.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
