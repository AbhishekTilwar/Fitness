{
  "name": "fitness-monorepo",
  "private": true,
  "scripts": {
    "build": "npm --prefix server run build",
    "start": "npm --prefix server start",
    "dev": "npm --prefix server run dev"
  }
}

